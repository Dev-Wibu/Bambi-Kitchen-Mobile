// Role types matching BE enum
export type ROLE_TYPE = "ADMIN" | "STAFF" | "USER";

export const ROLES = {
  ADMIN: "ADMIN" as ROLE_TYPE,
  STAFF: "STAFF" as ROLE_TYPE,
  USER: "USER" as ROLE_TYPE,
};
